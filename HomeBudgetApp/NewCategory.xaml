<Window x:Class="HomeBudgetApp.NewCategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeBudgetApp"
        mc:Ignorable="d"
        Title="NewCategory" MinHeight="450" MinWidth="800" Height="450" Width="800" WindowStartupLocation="CenterScreen" Background="{DynamicResource Background}">
    <Grid Margin="5">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox SelectionChanged="ComboBox_Themes_SelectionChanged" Margin="5" x:Name="ComboBox_Themes"></ComboBox>

        <TextBlock Foreground="{DynamicResource Text}" Grid.Column="1" Text="Create a New Category" FontWeight="Bold" FontSize="35" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Grid Grid.Row="1" Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Margin="5" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Foreground="{DynamicResource Text}" Grid.Row="0" FontSize="25" Text="Name" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBox Foreground="{DynamicResource Text}" Background="{DynamicResource TextBoxTheme}" x:Name="TextBox_CategoryName" FontSize="20" VerticalContentAlignment="Center" Grid.Row="1"/>
            </Grid>

            <Grid Margin="5" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Foreground="{DynamicResource Text}" Grid.Row="0" FontSize="25" Text="Type" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <ComboBox HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" x:Name="ComboBox_CategoryType" Grid.Row="1"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Foreground="{DynamicResource Text}"  Margin="10" Grid.Column="0" FontWeight="Bold" FontSize="20" Click="Btn_Cancel_Clicked">Cancel</Button>
            <Button Foreground="{DynamicResource Text}"  Margin="10" Grid.Column="1" FontWeight="Bold" FontSize="20" Click="CreateCategory_Click">Create</Button>
        </Grid>

    </Grid>
</Window>
