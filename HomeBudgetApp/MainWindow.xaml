<Window x:Class="HomeBudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeBudgetApp"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" MinHeight="600" MinWidth="750" Height="600" Width="750" Background="{DynamicResource Background}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <ComboBox SelectionChanged="ComboBox_Themes_SelectionChanged" Margin="5" x:Name="ComboBox_Themes"></ComboBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <TextBlock Foreground="{DynamicResource Text}" Grid.ColumnSpan="1" Grid.Column="3" Name="chosenDatabaseFile" />
            <TextBlock Foreground="{DynamicResource Text}" FontSize="30" HorizontalAlignment="Center" Grid.ColumnSpan="3" VerticalAlignment="Bottom">Welcome!</TextBlock>
            <Button Foreground="{DynamicResource Text}" FontSize="20" Grid.Row="1" Grid.Column="1" Margin="20" FontWeight="Bold" Click="Btn_File_Clicked">New/Open File</Button>
            <Button Foreground="{DynamicResource Text}" FontSize="20" Grid.Row="1" Margin="20" FontWeight="Bold" Click="ViewItem_Click">View Items</Button>
            <Button Foreground="{DynamicResource Text}" FontSize="20" Grid.Row="2" Margin="20, 70, 20, 70" FontWeight="Bold" Click="Btn_CatButton_Clicked">Add Category</Button>
            <Button Foreground="{DynamicResource Text}" FontSize="20" Grid.Row="2" Grid.Column="1" Margin="20, 70, 20, 70" FontWeight="Bold" Click="Btn_ExpButton_Clicked">Add Expense</Button>
            <Button Foreground="{DynamicResource Text}" FontSize="20" Grid.Row="2" Grid.Column="2" Margin="20, 70, 20, 70" FontWeight="Bold" Click="Btn_Close">Close</Button>
        </Grid>

    </Grid>
</Window>
